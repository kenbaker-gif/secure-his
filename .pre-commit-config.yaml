repos:
-   repo: local
    hooks:
    - id: check-secrets
      name: check committed files for common secrets
      entry: python3 scripts/check_secrets.py
      language: system
      types: [text, python, yaml, json, sh]
